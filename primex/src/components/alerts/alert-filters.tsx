"use client";

// Alert filter bar — reserved for Phase 2+ filter controls (severity, status, site, date range).
// Exported as a placeholder for now; will be wired up when filter state is introduced.

export function AlertFilters() {
  return null;
}
